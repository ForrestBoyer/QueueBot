Neccessary:

  Vote to skip
  - More than 1/2 of connected users
  - Threshold of certain ranks vote to skip

  Edit settings commands

  Change to role based

Cool for the future:

  Priority Queue

  Selective queues - Only certain ranks can join queue

Added:

  Users auto reentered into queue

  Queue messages only updated every 5 seconds to avoid rate limit

  Users removed from queue when leaving channel

  Queue message only updates if content has changed

  Queue auto starts when the first person joins

  Message next in queue 5 seconds before

  